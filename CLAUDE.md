# 项目概述
- Rust 语言实现常用工具集
- 工具以插件的形式实现，有统一的入口，聚合管理，插件可用快捷方式唤出
- GUI 形式，布局类似微软 PowerToys，图形库使用 egui

# 插件列表
- 密码管理工具，针对不同网站的账号密码存储记录
- 基于 Everything 的本地搜索
- hosts 管理工具，新增不同环境 hosts，启用禁用更环境 hosts
- JSON 编辑器，格式化处理，JSON 字符串解析；压缩、转义 JSON
- 其他插件后续补充添加

# 本地数据存储
- SQLite 优先